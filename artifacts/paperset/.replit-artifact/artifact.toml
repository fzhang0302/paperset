kind = "web"
previewPath = "/"
title = "Paperset"
version = "1.0.0"
id = "artifacts/paperset"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19603

[services.development]
run = "pnpm --filter @workspace/paperset run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/paperset", "run", "build" ]
publicDir = "artifacts/paperset/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19603"
BASE_PATH = "/"
